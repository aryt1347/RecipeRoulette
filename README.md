# RecipeRoulette
A web application built with the React framework that fetches and displays random recipes using the Tasty API. Users can explore new meal ideas with each search, as the app dynamically generates a fresh recipe suggestion based on a query.
